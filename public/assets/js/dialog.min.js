var dialogs=document.querySelectorAll("[aria-toggle]");void 0!==dialogs&&dialogs.forEach(dialog=>{dialog.addEventListener("click",()=>{const target=dialog.getAttribute("aria-toggle");let targetElement;document.querySelector(`[aria-dialog="${target}"]`).classList.replace("fade","show")})});var closeButtons=document.querySelectorAll("[aria-action]");void 0!==closeButtons&&closeButtons.forEach(closeButton=>{"close"===closeButton.getAttribute("aria-action")&&closeButton.addEventListener("click",()=>{document.querySelectorAll("[aria-dialog]").forEach(dialog=>{dialog.classList.contains("show")&&dialog.classList.replace("show","fade")})})});